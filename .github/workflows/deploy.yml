name: Deploy API to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, mobilegear]

    steps:
      - name: Deploy on EC2
        run: |
          set -e
          cd /home/ubuntu/apps/mobilegear
          git fetch origin
          git reset --hard origin/main
          docker compose up -d --build
          docker image prune -f
